<div class="space-y-8">
    <x-admin.page-header title="Apps" description="Manage integrations connected to this store." />
    <section class="space-y-4"><h2 class="text-lg font-semibold">Installed apps</h2>@if($this->installedApps->isEmpty())<x-admin.empty-state title="No apps installed" description="Install an available integration to extend your store." />@else<div class="grid gap-4 md:grid-cols-2">@foreach($this->installedApps as $installation)<article class="admin-card flex items-center gap-4 p-5"><div class="grid size-12 place-items-center rounded-xl bg-blue-50 text-blue-700"><flux:icon.squares-2x2 class="size-6" /></div><a href="{{ url('/admin/apps/'.$installation->id) }}" wire:navigate class="min-w-0 flex-1"><h3 class="font-semibold">{{ $installation->app->name }}</h3><p class="text-sm text-zinc-500">Installed {{ $installation->installed_at?->diffForHumans() ?? 'recently' }}</p></a><x-admin.status-badge :status="$installation->status" /></article>@endforeach</div>@endif</section>
    @if($this->availableApps->isNotEmpty())<section class="space-y-4"><h2 class="text-lg font-semibold">Available apps</h2><div class="grid gap-4 md:grid-cols-2">@foreach($this->availableApps as $app)<article class="admin-card flex items-center gap-4 p-5"><div class="grid size-12 place-items-center rounded-xl bg-zinc-100 text-zinc-600"><flux:icon.squares-2x2 class="size-6" /></div><div class="min-w-0 flex-1"><h3 class="font-semibold">{{ $app->name }}</h3><p class="text-sm text-zinc-500">Ready to install</p></div><flux:button type="button" wire:click="installApp({{ $app->id }})" size="sm">Install</flux:button></article>@endforeach</div></section>@endif
</div>
