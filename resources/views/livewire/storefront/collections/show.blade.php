<section class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><x-storefront.breadcrumbs :items="[['label' => 'Home', 'url' => route('storefront.home')], ['label' => 'Collections', 'url' => route('storefront.collections.index')], ['label' => $collection->title]]" /><h1 class="mt-6 text-4xl font-semibold">{{ $collection->title }}</h1><p class="mt-3 max-w-3xl text-zinc-600 dark:text-zinc-400">{{ strip_tags($collection->description_html ?? '') }}</p><div class="mt-8 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><flux:input wire:model.live.debounce.300ms="search" label="Filter products" placeholder="Search this collection" class="w-full sm:max-w-sm" /><flux:select wire:model.live="sort" label="Sort products" class="w-full sm:max-w-56"><flux:select.option value="newest">Newest</flux:select.option><flux:select.option value="price-low">Price: low to high</flux:select.option><flux:select.option value="price-high">Price: high to low</flux:select.option></flux:select></div><p aria-live="polite" class="mt-6 text-sm text-zinc-500">{{ $products->total() }} products</p><div class="mt-4 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">@forelse ($products as $product)<x-storefront.product-card wire:key="product-{{ $product->id }}" :product="$product" />@empty<p class="col-span-full rounded-xl bg-zinc-100 p-8 dark:bg-zinc-900">No products match your filters.</p>@endforelse</div><div class="mt-10">{{ $products->links() }}</div></section>
