<div class="mx-auto max-w-7xl px-4 py-14 lg:px-8"><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Explore</p><h1 class="mt-2 text-4xl font-bold">Collections</h1><div class="mt-10 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4">@foreach ($collections as $collection)<a wire:key="collection-{{ $collection->id }}" href="{{ route('collection.show', $collection->handle) }}" class="rounded-2xl border border-zinc-200 p-7 transition hover:border-blue-500 hover:shadow-lg dark:border-zinc-800" wire:navigate><h2 class="text-xl font-semibold">{{ $collection->title }}</h2><p class="mt-2 text-sm text-zinc-600 dark:text-zinc-400">{{ $collection->description }}</p><span class="mt-8 inline-block text-sm font-semibold text-blue-600">Shop now →</span></a>@endforeach</div></div>
