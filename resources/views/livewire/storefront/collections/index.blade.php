<section class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><h1 class="text-4xl font-semibold">Collections</h1><p class="mt-3 text-zinc-600 dark:text-zinc-400">Browse every collection from {{ $currentStore->name }}.</p><div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">@forelse ($collections as $collection)<a wire:key="collection-{{ $collection->id }}" href="{{ route('storefront.collections.show', $collection->handle) }}" class="rounded-2xl border border-zinc-200 p-8 dark:border-zinc-800"><h2 class="text-2xl font-medium">{{ $collection->title }}</h2><p class="mt-2 text-sm text-zinc-500">{{ $collection->products_count }} products</p></a>@empty<p>No collections available.</p>@endforelse</div></section>
