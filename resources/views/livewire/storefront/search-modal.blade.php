<div><flux:button wire:click="$set('open', true)" aria-label="Open product search">Search</flux:button><flux:modal wire:model="open" class="md:w-[36rem]"><div class="grid gap-4"><flux:heading size="lg">Search products</flux:heading><flux:input wire:model.live.debounce.200ms="query" label="Search" placeholder="What are you looking for?" autofocus />@if ($query !== '')<ul role="listbox" class="grid gap-1">@forelse ($suggestions as $product)<li wire:key="suggestion-{{ $product->id }}" role="option"><a class="block rounded-lg px-3 py-2 hover:bg-zinc-100 dark:hover:bg-zinc-800" href="{{ route('storefront.products.show', $product->handle) }}">{{ $product->title }}</a></li>@empty<li class="px-3 py-2 text-zinc-500">No suggestions</li>@endforelse</ul><flux:button :href="route('storefront.search', ['q' => $query])">View all results</flux:button>@endif</div></flux:modal></div>
