<div class="mx-auto max-w-7xl px-4 py-14 lg:px-8"><h1 class="text-4xl font-bold">Search</h1><form class="mt-8 flex max-w-xl gap-3" method="get" action="{{ route('search') }}"><label class="sr-only" for="search-query">Search products</label><input id="search-query" name="q" value="{{ $query }}" placeholder="Search products" class="flex-1 rounded-full border border-zinc-300 bg-white px-5 py-3 dark:border-zinc-700 dark:bg-zinc-900"><button class="rounded-full bg-zinc-900 px-6 py-3 font-semibold text-white dark:bg-white dark:text-zinc-900">Search</button></form>@if ($query !== '')<p class="mt-10 text-sm text-zinc-600 dark:text-zinc-400">Results for “{{ $query }}”</p>@endif<div class="mt-6 grid grid-cols-2 gap-x-4 gap-y-10 md:grid-cols-3 lg:grid-cols-4">@forelse ($products as $product)<x-storefront.product-card :product="$product" />@empty<div class="col-span-full py-20 text-center"><h2 class="text-xl font-semibold">No results</h2><p class="mt-2 text-zinc-600">Try a different search.</p></div>@endforelse</div><div class="mt-10">{{ $products->links() }}</div></div>
