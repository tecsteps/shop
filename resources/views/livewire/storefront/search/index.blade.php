<section class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><h1 class="text-4xl font-semibold">Search</h1><form class="mt-8" wire:submit="$refresh"><flux:input wire:model.live.debounce.300ms="query" label="Search products" placeholder="Try cotton, jeans, or shoes" /></form><p class="mt-6 text-sm text-zinc-500" aria-live="polite">{{ $products->total() }} results</p><div class="mt-4 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">@forelse ($products as $product)<x-storefront.product-card wire:key="search-product-{{ $product->id }}" :product="$product" />@empty<p class="col-span-full">No products found.</p>@endforelse</div><div class="mt-10">{{ $products->links() }}</div></section>
