<div>
    <section class="bg-zinc-100 dark:bg-zinc-900"><div class="mx-auto grid max-w-7xl gap-8 px-4 py-20 sm:px-6 lg:grid-cols-2 lg:px-8 lg:py-28"><div class="grid content-center gap-6"><x-storefront.badge>New season</x-storefront.badge><h1 class="text-4xl font-semibold tracking-tight sm:text-6xl">Find your next favorite.</h1><p class="max-w-xl text-lg text-zinc-600 dark:text-zinc-300">Explore curated essentials from {{ $currentStore->name }}.</p><div><flux:button variant="primary" :href="route('storefront.collections.index')" wire:navigate>Shop collections</flux:button></div></div><div class="aspect-[4/3] rounded-3xl bg-gradient-to-br from-zinc-300 to-zinc-100 dark:from-zinc-700 dark:to-zinc-900"></div></div></section>
    <section class="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><div class="flex items-end justify-between gap-4"><div><p class="text-sm font-medium uppercase tracking-wider text-zinc-500">Explore</p><h2 class="mt-2 text-3xl font-semibold">Featured collections</h2></div><a href="{{ route('storefront.collections.index') }}" class="text-sm underline">View all</a></div><div class="mt-8 grid gap-6 md:grid-cols-3">@forelse ($collections as $collection)<a wire:key="collection-{{ $collection->id }}" href="{{ route('storefront.collections.show', $collection->handle) }}" class="rounded-2xl border border-zinc-200 p-6 hover:bg-zinc-50 dark:border-zinc-800 dark:hover:bg-zinc-900"><h3 class="text-xl font-medium">{{ $collection->title }}</h3><p class="mt-2 text-sm text-zinc-500">{{ $collection->products_count }} products</p></a>@empty<p>No collections yet.</p>@endforelse</div></section>
    <section class="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><h2 class="text-3xl font-semibold">Featured products</h2><div class="mt-8 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">@foreach ($products as $product)<x-storefront.product-card wire:key="product-{{ $product->id }}" :product="$product" />@endforeach</div></section>
</div>
