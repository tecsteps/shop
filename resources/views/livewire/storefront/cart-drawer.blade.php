<div><flux:button wire:click="$set('open', true)" aria-label="Open shopping cart">Cart @if ($cart?->lines->count()) ({{ $cart->lines->sum('quantity') }}) @endif</flux:button><flux:modal wire:model="open" class="md:w-96"><div class="grid gap-5"><flux:heading size="lg">Shopping cart</flux:heading>@forelse ($cart?->lines ?? [] as $line)<div wire:key="drawer-line-{{ $line->id }}" class="flex justify-between gap-4 border-b border-zinc-200 pb-4 dark:border-zinc-800"><div><p class="font-medium">{{ $line->variant->product->title }}</p><p class="text-sm text-zinc-500">Qty {{ $line->quantity }}</p></div><flux:button size="sm" variant="ghost" wire:click="removeLine({{ $line->id }})">Remove</flux:button></div>@empty<p>Your cart is empty</p>@endforelse<flux:button variant="primary" :href="route('storefront.cart.show')" class="w-full">View cart</flux:button></div></flux:modal></div>
