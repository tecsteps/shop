<section class="mx-auto max-w-3xl px-4 py-16 text-center sm:px-6 lg:px-8"><div class="mx-auto flex size-16 items-center justify-center rounded-full bg-emerald-100 text-2xl text-emerald-700" aria-hidden="true">✓</div><h1 class="mt-6 text-4xl font-semibold">Thank you for your order!</h1><p class="mt-3 text-zinc-600 dark:text-zinc-400">Order {{ $order->order_number }} has been placed.</p>@if ($order->payment_method->value === 'bank_transfer')<flux:callout class="mt-8 text-left" variant="warning" heading="Bank transfer instructions"><p>Mock Bank AG · IBAN DE89 3704 0044 0532 0130 00 · BIC COBADEFFXXX</p><p class="mt-2">Use {{ $order->order_number }} as the payment reference.</p></flux:callout>@endif<div class="mt-8 rounded-2xl border border-zinc-200 p-6 text-left dark:border-zinc-800"><h2 class="text-xl font-semibold">Order details</h2>@foreach ($order->lines as $line)<div class="mt-4 flex justify-between gap-4"><span>{{ $line->quantity }} × {{ $line->title_snapshot }}</span><x-storefront.price :amount="$line->total_amount" :currency="$order->currency" /></div>@endforeach<div class="mt-5 flex justify-between border-t border-zinc-200 pt-5 font-semibold dark:border-zinc-800"><span>Total</span><x-storefront.price :amount="$order->total_amount" :currency="$order->currency" /></div></div><flux:button class="mt-8" :href="route('storefront.home')">Continue shopping</flux:button></section>
