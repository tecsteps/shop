<div class="mx-auto max-w-5xl px-4 py-14 lg:px-8"><div class="flex items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Shopping bag</p><h1 class="mt-2 text-4xl font-bold">Your Cart</h1></div><a href="{{ route('collections.index') }}" class="text-sm font-semibold underline" wire:navigate>Continue shopping</a></div>@if ($cart->lines->isEmpty())<div class="py-24 text-center"><div class="text-5xl text-zinc-300">⌂</div><h2 class="mt-6 text-2xl font-semibold">Your cart is empty</h2><p class="mt-2 text-zinc-600 dark:text-zinc-400">Add something you love to get started.</p><a href="{{ route('collections.index') }}" class="mt-6 inline-flex rounded-full bg-blue-600 px-6 py-3 font-semibold text-white" wire:navigate>Browse collections</a></div>@else<div class="mt-10 grid gap-10 lg:grid-cols-[1fr_340px]"><div class="divide-y divide-zinc-200 border-y border-zinc-200 dark:divide-zinc-800 dark:border-zinc-800">@foreach ($cart->lines as $line)<div wire:key="cart-line-{{ $line->id }}" class="flex gap-4 py-5"><div class="flex h-24 w-24 shrink-0 items-center justify-center rounded-xl bg-zinc-100 text-3xl dark:bg-zinc-800">⌂</div><div class="min-w-0 flex-1"><a href="{{ route('product.show', $line->variant->product->handle) }}" class="font-semibold" wire:navigate>{{ $line->variant->product->title }}</a><p class="mt-1 text-sm text-zinc-600 dark:text-zinc-400">{{ $line->variant->title }}</p><p class="mt-2 text-sm">€{{ number_format($line->unit_price_amount / 100, 2) }}</p><div class="mt-3 flex items-center gap-3"><button wire:click="decrease({{ $line->id }})" aria-label="Decrease quantity" class="h-8 w-8 rounded-full border">−</button><span>{{ $line->quantity }}</span><button wire:click="increase({{ $line->id }})" aria-label="Increase quantity" class="h-8 w-8 rounded-full border">+</button><button wire:click="remove({{ $line->id }})" class="ml-3 text-sm text-red-600">Remove</button></div></div><p class="font-semibold">€{{ number_format($line->line_total_amount / 100, 2) }}</p></div>@endforeach</div><aside class="h-fit rounded-2xl bg-zinc-50 p-6 dark:bg-zinc-900"><h2 class="text-xl font-semibold">Order summary</h2><form wire:submit="applyDiscount" class="mt-6 flex gap-2"><label class="sr-only" for="cart-discount">Discount code</label><input id="cart-discount" wire:model="discountCode" placeholder="Discount code" class="min-w-0 flex-1 rounded-lg border border-zinc-300 px-3 py-2 dark:border-zinc-700 dark:bg-zinc-950"><button class="rounded-lg border px-3 py-2 text-sm font-semibold">Apply</button></form>@error('discountCode')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror@if ($cart->discount_code)<p class="mt-3 text-sm text-green-600">{{ $cart->discount_code }} applied at checkout</p>@endif<div class="mt-6 flex justify-between text-sm"><span>Subtotal</span><span>€{{ number_format($cart->subtotalAmount() / 100, 2) }}</span></div><div class="mt-3 flex justify-between text-sm"><span>Shipping</span><span>Calculated at checkout</span></div><div class="mt-5 flex justify-between border-t border-zinc-200 pt-5 text-lg font-bold dark:border-zinc-700"><span>Total</span><span>€{{ number_format($cart->subtotalAmount() / 100, 2) }}</span></div><button wire:click="checkout" class="mt-6 w-full rounded-full bg-blue-600 px-5 py-3 font-semibold text-white">Checkout</button></aside></div>@endif</div>
