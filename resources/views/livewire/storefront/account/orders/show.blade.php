<div class="mx-auto max-w-4xl px-4 py-14 lg:px-8"><a href="{{ route('account.orders') }}" class="text-sm text-blue-600" wire:navigate>← Orders</a><h1 class="mt-4 text-4xl font-bold">{{ $order->order_number }}</h1><p class="mt-2 text-zinc-600">{{ ucfirst($order->financial_status->value) }} · {{ ucfirst($order->fulfillment_status->value) }}</p><div class="mt-8 divide-y divide-zinc-200 rounded-2xl border border-zinc-200 dark:divide-zinc-800 dark:border-zinc-800">@foreach ($order->lines as $line)<div wire:key="order-line-{{ $line->id }}" class="flex justify-between gap-4 p-5"><span>{{ $line->product_title }} · {{ $line->variant_title }} × {{ $line->quantity }}</span><strong>€{{ number_format($line->line_total_amount / 100, 2) }}</strong></div>@endforeach<div class="flex justify-between p-5 text-lg font-bold"><span>Total</span><span>€{{ number_format($order->total_amount / 100, 2) }}</span></div></div></div>
