<section class="mx-auto max-w-4xl px-4 py-12 sm:px-6 lg:px-8"><x-storefront.breadcrumbs :items="[['label' => 'Account', 'url' => route('storefront.account.dashboard')], ['label' => 'Orders', 'url' => route('storefront.account.orders.index')], ['label' => $order->order_number]]" /><div class="mt-6 flex flex-wrap justify-between gap-4"><div><h1 class="text-4xl font-semibold">Order {{ $order->order_number }}</h1><p class="mt-2 text-zinc-500">Placed {{ $order->placed_at?->format('M j, Y') }}</p></div><x-storefront.badge>{{ $order->status->value }}</x-storefront.badge></div><div class="mt-10 rounded-2xl border border-zinc-200 p-6 dark:border-zinc-800"><h2 class="text-xl font-semibold">Items</h2>@foreach ($order->lines as $line)<div class="mt-4 flex justify-between gap-4"><span>{{ $line->quantity }} × {{ $line->title_snapshot }}</span><x-storefront.price :amount="$line->total_amount" :currency="$order->currency" /></div>@endforeach<div class="mt-5 flex justify-between border-t border-zinc-200 pt-5 font-semibold dark:border-zinc-800"><span>Total</span><x-storefront.price :amount="$order->total_amount" :currency="$order->currency" /></div></div></section>
