<section class="mx-auto max-w-5xl px-4 py-12 sm:px-6 lg:px-8"><h1 class="text-4xl font-semibold">Your orders</h1><div class="mt-8 overflow-x-auto rounded-2xl border border-zinc-200 dark:border-zinc-800"><table class="w-full text-left"><thead class="bg-zinc-100 text-sm dark:bg-zinc-900"><tr><th class="p-4">Order</th><th class="p-4">Date</th><th class="p-4">Status</th><th class="p-4">Total</th></tr></thead><tbody>@forelse ($orders as $order)<tr wire:key="order-{{ $order->id }}" class="border-t border-zinc-200 dark:border-zinc-800"><td class="p-4"><a class="underline" href="{{ route('storefront.account.orders.show', $order->order_number) }}">{{ $order->order_number }}</a></td><td class="p-4">{{ $order->placed_at?->format('M j, Y') }}</td><td class="p-4">{{ $order->status->value }}</td><td class="p-4"><x-storefront.price :amount="$order->total_amount" :currency="$order->currency" /></td></tr>@empty<tr><td colspan="4" class="p-8 text-center">No orders yet.</td></tr>@endforelse</tbody></table></div><div class="mt-8">{{ $orders->links() }}</div></section>
