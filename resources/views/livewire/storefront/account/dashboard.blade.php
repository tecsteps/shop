<div class="mx-auto max-w-5xl px-4 py-14 lg:px-8"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Account</p><h1 class="mt-2 text-4xl font-bold">Welcome, {{ $customer->first_name }}</h1></div><form method="post" action="{{ route('account.logout') }}">@csrf<button class="text-sm font-semibold underline">Sign out</button></form></div><div class="mt-10 grid gap-4 sm:grid-cols-3"><a href="{{ route('account.orders') }}" class="rounded-2xl border border-zinc-200 p-6 dark:border-zinc-800"><p class="text-sm text-zinc-500">Orders</p><p class="mt-2 text-3xl font-bold">{{ $customer->orders_count }}</p></a><a href="{{ route('account.addresses') }}" class="rounded-2xl border border-zinc-200 p-6 dark:border-zinc-800"><p class="text-sm text-zinc-500">Addresses</p><p class="mt-2 text-3xl font-bold">{{ $customer->addresses()->count() }}</p></a><div class="rounded-2xl border border-zinc-200 p-6 dark:border-zinc-800"><p class="text-sm text-zinc-500">Email</p><p class="mt-2 truncate font-semibold">{{ $customer->email }}</p></div></div></div>
