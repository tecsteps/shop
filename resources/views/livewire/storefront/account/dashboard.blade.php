<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><div class="flex flex-wrap items-center justify-between gap-4"><div><h1 class="text-4xl font-semibold">Welcome, {{ $customer->name }}</h1><p class="mt-2 text-zinc-500">{{ $customer->email }}</p></div><flux:button wire:click="logout">Sign out</flux:button></div><div class="mt-10 grid gap-6 md:grid-cols-3"><a class="rounded-2xl border border-zinc-200 p-6 dark:border-zinc-800" href="{{ route('storefront.account.orders.index') }}"><h2 class="font-semibold">Orders</h2><p class="mt-2 text-sm text-zinc-500">View your order history</p></a><a class="rounded-2xl border border-zinc-200 p-6 dark:border-zinc-800" href="{{ route('storefront.account.addresses.index') }}"><h2 class="font-semibold">Addresses</h2><p class="mt-2 text-sm text-zinc-500">Manage delivery addresses</p></a></div><h2 class="mt-12 text-2xl font-semibold">Recent orders</h2><div class="mt-5 grid gap-3">@forelse ($orders as $order)<a wire:key="recent-order-{{ $order->id }}" class="flex justify-between rounded-xl border border-zinc-200 p-4 dark:border-zinc-800" href="{{ route('storefront.account.orders.show', $order->order_number) }}"><span>{{ $order->order_number }}</span><x-storefront.price :amount="$order->total_amount" :currency="$order->currency" /></a>@empty<p>No orders yet.</p>@endforelse</div></section>
