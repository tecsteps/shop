<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><h1 class="text-4xl font-semibold">Addresses</h1><div class="mt-8 grid gap-8 lg:grid-cols-2"><div class="grid content-start gap-4">@forelse ($addresses as $saved)<article wire:key="address-{{ $saved->id }}" class="rounded-2xl border border-zinc-200 p-5 dark:border-zinc-800"><div class="flex justify-between gap-4"><h2 class="font-semibold">{{ $saved->label ?: 'Address' }} @if ($saved->is_default)<x-storefront.badge>Default</x-storefront.badge>@endif</h2><div class="flex gap-2"><flux:button size="sm" wire:click="edit({{ $saved->id }})">Edit</flux:button><flux:button size="sm" variant="ghost" wire:click="delete({{ $saved->id }})">Delete</flux:button></div></div><address class="mt-3 text-sm not-italic text-zinc-600 dark:text-zinc-400">{{ $saved->address_json['first_name'] ?? '' }} {{ $saved->address_json['last_name'] ?? '' }}<br>{{ $saved->address_json['address1'] ?? '' }}<br>{{ $saved->address_json['zip'] ?? '' }} {{ $saved->address_json['city'] ?? '' }}</address></article>@empty<p>No saved addresses.</p>@endforelse</div><flux:card><flux:heading size="lg">{{ $editingAddressId ? 'Edit address' : 'Add address' }}</flux:heading><form wire:submit="save" class="mt-5 grid gap-4 sm:grid-cols-2"><flux:input wire:model="label" label="Label" /><div class="flex items-end"><flux:checkbox wire:model="isDefault" label="Default address" /></div><flux:input wire:model="address.first_name" label="First name" /><flux:input wire:model="address.last_name" label="Last name" /><flux:input class="sm:col-span-2" wire:model="address.address1" label="Address" /><flux:input wire:model="address.city" label="City" /><flux:input wire:model="address.zip" label="Postal code" /><flux:input wire:model="address.country_code" label="Country code" /><flux:input wire:model="address.phone" label="Phone" /><flux:button class="sm:col-span-2" type="submit" variant="primary">Save address</flux:button></form></flux:card></div></section>
