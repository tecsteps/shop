<div class="space-y-6">
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Online store</p><h1 class="mt-2 text-3xl font-bold">Pages</h1><p class="mt-2 text-zinc-600">Publish the content pages linked from your storefront.</p></div><flux:button variant="primary" :href="route('admin.pages.create')" wire:navigate>Create page</flux:button></div>
    <div class="flex flex-wrap gap-3"><flux:input wire:model.live.debounce.300ms="search" placeholder="Search pages" aria-label="Search pages" /><flux:select wire:model.live="status" aria-label="Filter pages"><option value="all">All statuses</option><option value="published">Published</option><option value="draft">Draft</option></flux:select></div>
    <div class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-zinc-200"><table class="min-w-full divide-y divide-zinc-200 text-left text-sm"><thead class="bg-zinc-50"><tr><th class="px-5 py-3">Page</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Updated</th><th class="px-5 py-3"><span class="sr-only">Actions</span></th></tr></thead><tbody class="divide-y divide-zinc-100">@forelse($pages as $page)<tr wire:key="page-{{ $page->id }}"><td class="px-5 py-4"><a class="font-semibold hover:underline" href="{{ route('admin.pages.edit', $page) }}" wire:navigate>{{ $page->title }}</a><div class="text-xs text-zinc-500">/pages/{{ $page->handle }}</div></td><td class="px-5 py-4">{{ ucfirst($page->status->value) }}</td><td class="px-5 py-4">{{ $page->updated_at?->diffForHumans() }}</td><td class="px-5 py-4 text-right"><flux:button size="sm" variant="ghost" wire:click="delete({{ $page->id }})" wire:confirm="Delete this page?">Delete</flux:button></td></tr>@empty<tr><td colspan="4" class="px-5 py-12 text-center text-zinc-500">No pages match your filters.</td></tr>@endforelse</tbody></table></div><div>{{ $pages->links() }}</div>
</div>
