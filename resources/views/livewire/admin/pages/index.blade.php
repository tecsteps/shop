<div class="flex flex-col gap-6"><div class="flex items-center justify-between gap-4"><div><flux:breadcrumbs><flux:breadcrumbs.item :href="route('admin.dashboard')">Home</flux:breadcrumbs.item><flux:breadcrumbs.item>Pages</flux:breadcrumbs.item></flux:breadcrumbs><flux:heading size="xl">Pages</flux:heading></div><flux:button :href="route('admin.pages.create')" wire:navigate variant="primary" icon="plus">Add page</flux:button></div><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search pages..." /><div class="overflow-x-auto rounded-xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900"><table class="w-full text-sm"><thead><tr class="text-left text-zinc-500"><th class="p-3">Title</th><th>Handle</th><th>Status</th><th>Updated</th></tr></thead><tbody>@forelse ($pages as $page)<tr wire:key="page-{{ $page->id }}" class="border-t border-zinc-100 dark:border-zinc-800"><td class="p-3"><a href="{{ route('admin.pages.edit', $page) }}" wire:navigate class="font-medium">{{ $page->title }}</a></td><td>{{ $page->handle }}</td><td><flux:badge size="sm">{{ ucfirst($page->status->value) }}</flux:badge></td><td>{{ $page->updated_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="4" class="p-10 text-center">No pages found.</td></tr>@endforelse</tbody></table></div>{{ $pages->links() }}</div>