<x-admin.page title="Inventory" subtitle="Track on-hand and reserved stock across variants.">
    <x-admin.panel><div class="grid gap-3 md:grid-cols-[1fr_14rem]"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search by product or SKU" aria-label="Search inventory" /><flux:select wire:model.live="stockFilter" aria-label="Filter stock"><flux:select.option value="all">All stock</flux:select.option><flux:select.option value="in_stock">In stock</flux:select.option><flux:select.option value="low_stock">Low stock</flux:select.option><flux:select.option value="out_of_stock">Out of stock</flux:select.option></flux:select></div></x-admin.panel>
    <x-admin.panel class="overflow-hidden p-0"><div class="overflow-x-auto"><table class="w-full min-w-[48rem] text-sm"><thead class="bg-zinc-50 text-left text-zinc-500 dark:bg-zinc-950/50"><tr><th class="p-4">Product</th><th class="p-4">Variant</th><th class="p-4">SKU</th><th class="p-4">On hand</th><th class="p-4">Reserved</th><th class="p-4">Policy</th></tr></thead><tbody class="divide-y divide-zinc-200 dark:divide-zinc-800">@forelse($this->inventoryItems as $item)<tr wire:key="inventory-{{ $item->id }}"><td class="p-4 font-medium">{{ $item->variant->product->title }}</td><td class="p-4">{{ $item->variant->optionValues->pluck('value')->implode(' / ') ?: 'Default' }}</td><td class="p-4">{{ $item->variant->sku ?: '—' }}</td><td class="p-4"><flux:input type="number" min="0" value="{{ $item->quantity_on_hand }}" class="w-24" aria-label="On hand for {{ $item->variant->sku }}" wire:change="updateQuantity({{ $item->id }}, $event.target.value)" /></td><td class="p-4">{{ $item->quantity_reserved }}</td><td class="p-4"><flux:badge :color="$item->policy->value === 'continue' ? 'amber' : 'zinc'">{{ $item->policy->value }}</flux:badge></td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-zinc-500">No inventory items found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-zinc-200 p-4 dark:border-zinc-800">{{ $this->inventoryItems->links() }}</div></x-admin.panel>
</x-admin.page>
