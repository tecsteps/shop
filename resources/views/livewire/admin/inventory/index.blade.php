<div class="space-y-6"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Catalog</p><h1 class="mt-2 text-3xl font-bold">Inventory</h1><p class="mt-2 text-zinc-600">Track on-hand, reserved, and available quantities by variant.</p></div><div class="flex flex-wrap gap-3"><flux:input wire:model.live.debounce.300ms="search" placeholder="Search products or SKU" aria-label="Search inventory" /><flux:select wire:model.live="stock" aria-label="Filter stock"><option value="all">All stock</option><option value="low">Low stock</option><option value="out">Out of stock</option></flux:select></div><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-zinc-200"><table class="min-w-full divide-y divide-zinc-200 text-left text-sm"><thead class="bg-zinc-50"><tr><th class="px-5 py-3">Product / variant</th><th class="px-5 py-3">SKU</th><th class="px-5 py-3">On hand</th><th class="px-5 py-3">Reserved</th><th class="px-5 py-3">Policy</th><th class="px-5 py-3"><span class="sr-only">Save</span></th></tr></thead><tbody class="divide-y divide-zinc-100">@forelse($items as $item)<tr wire:key="inventory-{{ $item->id }}"><td class="px-5 py-4"><div class="font-semibold">{{ $item->variant?->product?->title }}</div><div class="text-xs text-zinc-500">{{ $item->variant?->title }}</div></td><td class="px-5 py-4 text-zinc-600">{{ $item->variant?->sku ?: '—' }}</td><td class="px-5 py-4"><flux:input type="number" min="0" class="w-24" wire:model="quantities.{{ $item->id }}" aria-label="On hand for {{ $item->variant?->title }}" /></td><td class="px-5 py-4">{{ $item->quantity_reserved }}</td><td class="px-5 py-4"><flux:select wire:model="policies.{{ $item->id }}" aria-label="Policy for {{ $item->variant?->title }}"><option value="deny">Deny</option><option value="continue">Continue</option></flux:select></td><td class="px-5 py-4 text-right"><flux:button size="sm" variant="primary" wire:click="save({{ $item->id }})">Save</flux:button></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-zinc-500">No inventory matches your filters.</td></tr>@endforelse</tbody></table></div><div>{{ $items->links() }}</div></div>
