<div class="flex flex-col gap-6"><div><flux:breadcrumbs><flux:breadcrumbs.item :href="route('admin.dashboard')">Home</flux:breadcrumbs.item><flux:breadcrumbs.item>Inventory</flux:breadcrumbs.item></flux:breadcrumbs><flux:heading size="xl">Inventory</flux:heading></div><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search product or SKU..." /><div class="overflow-x-auto rounded-xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900"><table class="w-full text-sm"><thead><tr class="text-left text-zinc-500"><th class="p-3">Product</th><th>SKU</th><th>On hand</th><th>Reserved</th><th>Available</th></tr></thead><tbody>@forelse ($items as $item)<tr wire:key="inventory-{{ $item->id }}" class="border-t border-zinc-100 dark:border-zinc-800"><td class="p-3">{{ $item->variant->product->title }}</td><td>{{ $item->variant->sku ?: '—' }}</td><td>{{ $item->quantity_on_hand }}</td><td>{{ $item->quantity_reserved }}</td><td>{{ $item->quantity_on_hand - $item->quantity_reserved }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center">No inventory found.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div>