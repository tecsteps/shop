<div class="flex h-16 items-center justify-between gap-3 px-4 sm:px-6">
    <div class="flex items-center gap-2"><flux:button class="lg:hidden" variant="ghost" icon="bars-3" aria-label="Open navigation" @click="menuOpen = true" /><flux:dropdown><flux:button variant="ghost" icon:trailing="chevron-down">{{ $currentStoreName }}</flux:button><flux:menu>@foreach ($stores as $store)<flux:menu.item wire:key="store-{{ $store->id }}" wire:click="switchStore({{ $store->id }})">{{ $store->name }}</flux:menu.item>@endforeach</flux:menu></flux:dropdown></div>
    <div class="flex items-center gap-2"><div class="relative"><flux:button variant="ghost" icon="bell" aria-label="Notifications" />@if($unreadNotificationCount)<flux:badge size="sm" color="red" class="absolute -end-1 -top-1">{{ $unreadNotificationCount }}</flux:badge>@endif</div><flux:dropdown position="bottom" align="end"><flux:profile :name="auth()->user()->name" :initials="auth()->user()->initials()" icon:trailing="chevron-down" /><flux:menu><flux:menu.item href="/admin/settings" wire:navigate icon="cog-6-tooth">Settings</flux:menu.item><flux:menu.separator /><flux:menu.item wire:click="logout" icon="arrow-right-start-on-rectangle">Log out</flux:menu.item></flux:menu></flux:dropdown></div>
</div>
