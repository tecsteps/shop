<div><div class="flex items-end justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Marketing</p><h1 class="mt-2 text-3xl font-bold">Discounts</h1></div><a href="{{ route('admin.discounts.create') }}" class="rounded-full bg-blue-600 px-5 py-3 text-sm font-semibold text-white" wire:navigate>Create discount</a></div><div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-zinc-50"><tr><th class="px-5 py-4">Code</th><th class="px-5 py-4">Value</th><th class="px-5 py-4">Status</th></tr></thead><tbody class="divide-y divide-zinc-200">@foreach ($discounts as $discount)<tr wire:key="discount-{{ $discount->id }}"><td class="px-5 py-4 font-semibold">{{ $discount->code }}</td><td class="px-5 py-4">{{ $discount->value_type->value === 'percent' ? $discount->value_amount.'%' : ($discount->value_type->value === 'fixed' ? '€'.number_format($discount->value_amount / 100, 2) : 'Free shipping') }}</td><td class="px-5 py-4">{{ $discount->isAvailable() ? 'Active' : 'Expired' }}</td></tr>@endforeach</tbody></table></div></div>
