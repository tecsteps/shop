<div class="flex flex-col gap-6"><div class="flex items-center justify-between gap-4"><div><flux:breadcrumbs><flux:breadcrumbs.item :href="route('admin.dashboard')">Home</flux:breadcrumbs.item><flux:breadcrumbs.item>Discounts</flux:breadcrumbs.item></flux:breadcrumbs><flux:heading size="xl">Discounts</flux:heading></div><flux:button :href="route('admin.discounts.create')" wire:navigate variant="primary" icon="plus">Create discount</flux:button></div><div class="grid gap-3 sm:grid-cols-2"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search code..." /><flux:select wire:model.live="statusFilter"><flux:select.option value="all">All statuses</flux:select.option><flux:select.option value="active">Active</flux:select.option><flux:select.option value="expired">Expired</flux:select.option><flux:select.option value="disabled">Disabled</flux:select.option></flux:select></div><div class="overflow-x-auto rounded-xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900"><table class="w-full text-sm"><thead><tr class="text-left text-zinc-500"><th class="p-3">Code</th><th>Type</th><th>Value</th><th>Usage</th><th>Status</th><th>Dates</th></tr></thead><tbody>@forelse ($discounts as $discount)<tr wire:key="discount-{{ $discount->id }}" class="border-t border-zinc-100 dark:border-zinc-800"><td class="p-3"><a href="{{ route('admin.discounts.edit', $discount) }}" wire:navigate class="font-medium">{{ $discount->code ?: 'Automatic' }}</a></td><td><flux:badge size="sm">{{ ucfirst($discount->type->value) }}</flux:badge></td><td>{{ $discount->value_type->value === 'percent' ? $discount->value_amount.'%' : ($discount->value_type->value === 'free_shipping' ? 'Free shipping' : number_format($discount->value_amount / 100, 2)) }}</td><td>{{ $discount->usage_count }} / {{ $discount->usage_limit ?? 'unlimited' }}</td><td><flux:badge size="sm" :color="$discount->status->value === 'active' ? 'green' : 'zinc'">{{ ucfirst($discount->status->value) }}</flux:badge></td><td>{{ $discount->starts_at?->format('M j, Y') }} – {{ $discount->ends_at?->format('M j, Y') ?? 'No end' }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No discounts found.</td></tr>@endforelse</tbody></table></div>{{ $discounts->links() }}</div>