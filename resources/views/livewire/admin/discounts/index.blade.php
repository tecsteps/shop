<x-admin.page title="Discounts" subtitle="Create promotions and track redemptions.">
    <x-slot:actions><flux:button href="/admin/discounts/create" wire:navigate variant="primary" icon="plus">Add discount</flux:button></x-slot:actions>
    <x-admin.panel><div class="grid gap-3 md:grid-cols-[1fr_14rem]"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search discount codes…" aria-label="Search discounts" /><flux:select wire:model.live="statusFilter" aria-label="Filter discounts"><flux:select.option value="all">All statuses</flux:select.option><flux:select.option value="active">Active</flux:select.option><flux:select.option value="draft">Draft</flux:select.option><flux:select.option value="expired">Expired</flux:select.option><flux:select.option value="disabled">Disabled</flux:select.option></flux:select></div></x-admin.panel>
    <x-admin.panel class="overflow-hidden p-0"><div class="overflow-x-auto"><table class="w-full min-w-[48rem] text-sm"><thead class="bg-zinc-50 text-left text-zinc-500 dark:bg-zinc-950/50"><tr><th class="p-4">Code</th><th class="p-4">Value</th><th class="p-4">Status</th><th class="p-4">Uses</th><th class="p-4">Ends</th><th class="p-4"><span class="sr-only">Actions</span></th></tr></thead><tbody class="divide-y divide-zinc-200 dark:divide-zinc-800">@forelse($this->discounts as $discount)<tr wire:key="discount-{{ $discount->id }}"><td class="p-4"><a href="/admin/discounts/{{ $discount->id }}/edit" wire:navigate class="font-semibold hover:underline">{{ $discount->code }}</a></td><td class="p-4">@if($discount->value_type->value === 'percent'){{ $discount->value_amount }}%@elseif($discount->value_type->value === 'fixed'){{ \Illuminate\Support\Number::currency($discount->value_amount / 100, in: $this->currentStore()->default_currency) }}@else Free shipping @endif</td><td class="p-4"><flux:badge :color="$discount->status->value === 'active' ? 'green' : ($discount->status->value === 'expired' ? 'red' : 'zinc')">{{ str($discount->status->value)->headline() }}</flux:badge></td><td class="p-4">{{ $discount->usage_count }} / {{ $discount->usage_limit ?: '∞' }}</td><td class="p-4">{{ $discount->ends_at?->format('M j, Y') ?: 'Never' }}</td><td class="p-4 text-end"><flux:button wire:click="toggleStatus({{ $discount->id }})" size="sm">{{ $discount->status->value === 'active' ? 'Disable' : 'Activate' }}</flux:button></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-zinc-500">No discounts found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-zinc-200 p-4 dark:border-zinc-800">{{ $this->discounts->links() }}</div></x-admin.panel>
</x-admin.page>
