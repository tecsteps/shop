<div class="space-y-6">
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Marketing</p><flux:heading size="xl" level="1" class="mt-2">Discounts</flux:heading><flux:text class="mt-2" variant="subtle">Create offers, target products, and monitor usage.</flux:text></div><flux:button variant="primary" :href="route('admin.discounts.create')" wire:navigate>Create discount</flux:button></div>
    <div class="flex flex-wrap gap-3"><flux:input wire:model.live.debounce.300ms="search" placeholder="Search discount code…" aria-label="Search discounts" /><flux:select wire:model.live="statusFilter" aria-label="Filter discounts"><option value="all">All statuses</option><option value="active">Active</option><option value="scheduled">Scheduled</option><option value="expired">Expired</option></flux:select></div>
    <div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-zinc-200"><table class="min-w-full divide-y divide-zinc-200 text-left text-sm"><caption class="sr-only">Discounts</caption><thead class="bg-zinc-50"><tr><th class="px-5 py-3">Code</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Value</th><th class="px-5 py-3">Usage</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Dates</th><th class="px-5 py-3"><span class="sr-only">Actions</span></th></tr></thead><tbody class="divide-y divide-zinc-100">@forelse ($discounts as $discount) @php($discountStatus = $discount->starts_at?->isFuture() ? 'scheduled' : (($discount->ends_at?->isPast() || ($discount->usage_limit !== null && $discount->usage_count >= $discount->usage_limit)) ? 'expired' : ($discount->status === 'active' ? 'active' : 'disabled'))) <tr wire:key="discount-{{ $discount->id }}" class="hover:bg-zinc-50"><td class="px-5 py-4"><a href="{{ route('admin.discounts.edit', $discount) }}" wire:navigate class="font-semibold text-blue-700 hover:underline">{{ $discount->code ?: 'Automatic' }}</a></td><td class="px-5 py-4"><flux:badge color="zinc">{{ $discount->type->value === 'automatic' ? 'Automatic' : 'Code' }}</flux:badge></td><td class="px-5 py-4">{{ $discount->value_type->value === 'percent' ? $discount->value_amount.'%' : ($discount->value_type->value === 'fixed' ? app('current_store')->default_currency.' '.number_format($discount->value_amount / 100, 2) : 'Free shipping') }}</td><td class="px-5 py-4">{{ $discount->usage_count }} / {{ $discount->usage_limit ?? 'unlimited' }}</td><td class="px-5 py-4"><flux:badge :color="$discountStatus === 'active' ? 'green' : ($discountStatus === 'scheduled' ? 'yellow' : ($discountStatus === 'expired' ? 'red' : 'zinc'))">{{ ucfirst($discountStatus) }}</flux:badge></td><td class="whitespace-nowrap px-5 py-4 text-zinc-600"><div>{{ $discount->starts_at?->format('M j, Y') ?? 'Any time' }}</div><div>{{ $discount->ends_at?->format('M j, Y') ?? 'No end date' }}</div></td><td class="px-5 py-4 text-right"><flux:button size="sm" variant="ghost" wire:click="delete({{ $discount->id }})" wire:confirm="Delete this discount?">Delete</flux:button></td></tr> @empty<tr><td colspan="7" class="px-5 py-12 text-center text-zinc-500">No discounts match these filters.</td></tr>@endforelse</tbody></table></div>
    <div>{{ $discounts->links() }}</div>
</div>
