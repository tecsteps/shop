<div class="flex flex-col gap-6">
    <div><flux:breadcrumbs><flux:breadcrumbs.item>Home</flux:breadcrumbs.item></flux:breadcrumbs><flux:heading size="xl">Dashboard</flux:heading><flux:text>Store performance at a glance.</flux:text></div>
    <div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach ([['Revenue', $revenue, true], ['Orders', $orderCount, false], ['Average order', $averageOrderValue, true], ['Customers', $customerCount, false]] as [$label, $value, $money])<section wire:key="kpi-{{ $label }}" class="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><flux:text>{{ $label }}</flux:text><p class="mt-2 text-2xl font-semibold">{{ $money ? number_format($value / 100, 2).' '.app('current_store')->default_currency : number_format($value) }}</p></section>@endforeach</div>
    <section class="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><div class="flex items-center justify-between gap-4"><flux:heading size="lg">Recent orders</flux:heading><flux:button :href="route('admin.orders.index')" wire:navigate variant="ghost">View all</flux:button></div><div class="mt-4 overflow-x-auto"><table class="w-full text-sm"><thead class="text-left text-zinc-500"><tr><th class="py-2">Order</th><th>Customer</th><th>Status</th><th class="text-right">Total</th></tr></thead><tbody>@forelse ($recentOrders as $order)<tr wire:key="order-{{ $order->id }}" class="border-t border-zinc-100 dark:border-zinc-800"><td class="py-3"><a href="{{ route('admin.orders.show', $order) }}" wire:navigate class="font-medium">{{ $order->order_number }}</a></td><td>{{ $order->customer?->name ?? $order->email }}</td><td><flux:badge size="sm">{{ ucfirst($order->status->value) }}</flux:badge></td><td class="text-right">{{ number_format($order->total_amount / 100, 2) }} {{ $order->currency }}</td></tr>@empty<tr><td colspan="4" class="py-8 text-center text-zinc-500">No orders yet.</td></tr>@endforelse</tbody></table></div></section>
</div>