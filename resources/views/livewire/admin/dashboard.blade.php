<div>
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Overview</p><h1 class="mt-2 text-3xl font-bold">Dashboard</h1><p class="mt-2 text-sm text-zinc-600 dark:text-zinc-400">Performance for the selected period.</p></div><div class="flex items-center gap-3"><label for="dashboard-range" class="text-sm font-medium">Period</label><select id="dashboard-range" wire:model.live="range" class="rounded-lg border border-zinc-300 bg-white px-3 py-2 dark:border-zinc-700 dark:bg-zinc-900"><option value="7">Last 7 days</option><option value="30">Last 30 days</option><option value="90">Last 90 days</option></select><a href="{{ route('admin.products.create') }}" class="rounded-full bg-blue-600 px-5 py-3 text-sm font-semibold text-white" wire:navigate>Add product</a></div></div>
    <div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-5"><div class="rounded-2xl bg-white p-5 shadow-sm dark:bg-zinc-900"><p class="text-sm text-zinc-500">Total sales</p><p class="mt-2 text-3xl font-bold">€{{ number_format($sales / 100, 2) }}</p></div><div class="rounded-2xl bg-white p-5 shadow-sm dark:bg-zinc-900"><p class="text-sm text-zinc-500">Orders</p><p class="mt-2 text-3xl font-bold">{{ $orderCount }}</p></div><div class="rounded-2xl bg-white p-5 shadow-sm dark:bg-zinc-900"><p class="text-sm text-zinc-500">Visitors</p><p class="mt-2 text-3xl font-bold">{{ number_format($visitors) }}</p></div><div class="rounded-2xl bg-white p-5 shadow-sm dark:bg-zinc-900"><p class="text-sm text-zinc-500">Add to cart</p><p class="mt-2 text-3xl font-bold">{{ number_format($addToCart) }}</p></div><div class="rounded-2xl bg-white p-5 shadow-sm dark:bg-zinc-900"><p class="text-sm text-zinc-500">Average order value</p><p class="mt-2 text-3xl font-bold">€{{ number_format($orderCount ? ($sales / $orderCount) / 100 : 0, 2) }}</p></div></div>
    <div class="mt-8 grid gap-8 lg:grid-cols-[minmax(0,1.5fr)_minmax(280px,1fr)]"><section class="rounded-2xl bg-white p-5 shadow-sm dark:bg-zinc-900" aria-labelledby="sales-chart-heading"><div class="flex items-center justify-between"><h2 id="sales-chart-heading" class="font-semibold">Sales trend</h2><span class="text-sm text-zinc-500">{{ $analytics->count() }} recorded days</span></div><div class="mt-6 flex h-48 items-end gap-1 border-b border-l border-zinc-200 px-2 pb-1 dark:border-zinc-700" role="img" aria-label="Daily sales trend"><div class="flex h-full w-full items-end gap-1">@php($maxRevenue = max(1, (int) $analytics->max('revenue_amount'))) @forelse ($analytics as $day)<div class="group flex h-full flex-1 items-end" title="{{ $day->date?->toFormattedDateString() }}: €{{ number_format($day->revenue_amount / 100, 2) }}"><div class="w-full rounded-t bg-blue-500 transition group-hover:bg-blue-700" style="height: {{ max(2, (int) (($day->revenue_amount / $maxRevenue) * 100)) }}%"></div></div>@empty<div class="flex w-full items-center justify-center text-sm text-zinc-500">No analytics recorded for this period.</div>@endforelse</div></div><div class="mt-3 flex justify-between text-xs text-zinc-500"><span>{{ $analytics->first()?->date?->toFormattedDateString() ?? 'Start' }}</span><span>Revenue in EUR</span></div></section><section class="rounded-2xl bg-white p-5 shadow-sm dark:bg-zinc-900" aria-labelledby="funnel-heading"><h2 id="funnel-heading" class="font-semibold">Conversion funnel</h2><dl class="mt-5 space-y-4 text-sm"><div><div class="flex justify-between"><dt>Visitors</dt><dd class="font-semibold">{{ number_format($visitors) }}</dd></div><div class="mt-2 h-2 rounded-full bg-zinc-100 dark:bg-zinc-800"><div class="h-2 rounded-full bg-blue-500" style="width:100%"></div></div></div><div><div class="flex justify-between"><dt>Added to cart</dt><dd class="font-semibold">{{ number_format($addToCart) }}</dd></div><div class="mt-2 h-2 rounded-full bg-zinc-100 dark:bg-zinc-800"><div class="h-2 rounded-full bg-blue-500" style="width:{{ $visitors ? min(100, ($addToCart / $visitors) * 100) : 0 }}%"></div></div></div><div><div class="flex justify-between"><dt>Checkout started</dt><dd class="font-semibold">{{ number_format($checkoutStarted) }}</dd></div><div class="mt-2 h-2 rounded-full bg-zinc-100 dark:bg-zinc-800"><div class="h-2 rounded-full bg-blue-500" style="width:{{ $visitors ? min(100, ($checkoutStarted / $visitors) * 100) : 0 }}%"></div></div></div><div><div class="flex justify-between"><dt>Orders</dt><dd class="font-semibold">{{ number_format($orderCount) }}</dd></div><div class="mt-2 h-2 rounded-full bg-zinc-100 dark:bg-zinc-800"><div class="h-2 rounded-full bg-blue-500" style="width:{{ $visitors ? min(100, ($orderCount / $visitors) * 100) : 0 }}%"></div></div></div></dl></section></div>
    <div class="mt-8 grid gap-8 lg:grid-cols-2"><section class="rounded-2xl bg-white shadow-sm dark:bg-zinc-900"><div class="flex items-center justify-between border-b border-zinc-200 px-5 py-4 dark:border-zinc-800"><h2 class="font-semibold">Recent orders</h2><a href="{{ route('admin.orders.index') }}" class="text-sm text-blue-600" wire:navigate>View all</a></div><div class="divide-y divide-zinc-200 dark:divide-zinc-800">@forelse ($orders as $order)<a wire:key="dashboard-order-{{ $order->id }}" href="{{ route('admin.orders.show', $order) }}" class="flex items-center justify-between px-5 py-4 hover:bg-zinc-50 dark:hover:bg-zinc-800" wire:navigate><span><span class="font-semibold">{{ $order->order_number }}</span><span class="ml-3 text-sm text-zinc-500">{{ $order->customer?->name ?? $order->email }}</span></span><span class="text-sm font-semibold">€{{ number_format($order->total_amount / 100, 2) }}</span></a>@empty<p class="px-5 py-8 text-sm text-zinc-500">No orders yet.</p>@endforelse</div></section><section class="rounded-2xl bg-white p-5 shadow-sm dark:bg-zinc-900" aria-labelledby="top-products-heading"><h2 id="top-products-heading" class="font-semibold">Top products</h2><div class="mt-4 space-y-3">@forelse ($topProducts as $product)<div class="flex items-center justify-between gap-4 text-sm"><span class="min-w-0 truncate">{{ $product->product_title }}</span><span class="shrink-0 text-zinc-500">{{ $product->units }} sold · €{{ number_format($product->revenue / 100, 2) }}</span></div>@empty<p class="py-5 text-sm text-zinc-500">No product sales yet.</p>@endforelse</div></section></div>
</div>
