<div><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Overview</p><h1 class="mt-2 text-3xl font-bold">Dashboard</h1></div><a href="{{ route('admin.products.create') }}" class="rounded-full bg-blue-600 px-5 py-3 text-sm font-semibold text-white" wire:navigate>Add product</a></div><div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div class="rounded-2xl bg-white p-5 shadow-sm"><p class="text-sm text-zinc-500">Total sales</p><p class="mt-2 text-3xl font-bold">€{{ number_format($sales / 100, 2) }}</p></div><div class="rounded-2xl bg-white p-5 shadow-sm"><p class="text-sm text-zinc-500">Orders</p><p class="mt-2 text-3xl font-bold">{{ $orderCount }}</p></div><div class="rounded-2xl bg-white p-5 shadow-sm"><p class="text-sm text-zinc-500">Products</p><p class="mt-2 text-3xl font-bold">{{ $productCount }}</p></div><div class="rounded-2xl bg-white p-5 shadow-sm"><p class="text-sm text-zinc-500">Average order value</p><p class="mt-2 text-3xl font-bold">€{{ number_format($orderCount ? ($sales / $orderCount) / 100 : 0, 2) }}</p></div></div><div class="mt-8 rounded-2xl bg-white shadow-sm"><div class="flex items-center justify-between border-b border-zinc-200 px-5 py-4"><h2 class="font-semibold">Recent orders</h2><a href="{{ route('admin.orders.index') }}" class="text-sm text-blue-600" wire:navigate>View all</a></div><div class="divide-y divide-zinc-200">@forelse ($orders as $order)<a wire:key="dashboard-order-{{ $order->id }}" href="{{ route('admin.orders.show', $order) }}" class="flex items-center justify-between px-5 py-4 hover:bg-zinc-50" wire:navigate><span><span class="font-semibold">{{ $order->order_number }}</span><span class="ml-3 text-sm text-zinc-500">{{ $order->customer?->name ?? $order->email }}</span></span><span class="text-sm font-semibold">€{{ number_format($order->total_amount / 100, 2) }}</span></a>@empty<p class="px-5 py-8 text-sm text-zinc-500">No orders yet.</p>@endforelse</div></div></div>
