<x-admin.page title="Products" subtitle="Manage catalog products, variants, and publishing.">
    <x-slot:actions><flux:button href="/admin/products/create" wire:navigate variant="primary" icon="plus">Add product</flux:button></x-slot:actions>
    <x-admin.panel><div class="grid gap-3 lg:grid-cols-[1fr_12rem_12rem]"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search products…" aria-label="Search products" /><flux:select wire:model.live="statusFilter" aria-label="Filter status"><flux:select.option value="all">All statuses</flux:select.option><flux:select.option value="active">Active</flux:select.option><flux:select.option value="draft">Draft</flux:select.option><flux:select.option value="archived">Archived</flux:select.option></flux:select><flux:select wire:model.live="typeFilter" aria-label="Filter product type"><flux:select.option value="all">All types</flux:select.option>@foreach($this->productTypes as $type)<flux:select.option wire:key="type-{{ $type }}" value="{{ $type }}">{{ $type }}</flux:select.option>@endforeach</flux:select></div></x-admin.panel>
    @if($selectedIds)<div class="flex flex-wrap items-center gap-2 rounded-xl border border-blue-200 bg-blue-50 p-3 dark:border-blue-900 dark:bg-blue-950/40"><span class="me-auto text-sm font-medium">{{ count($selectedIds) }} products selected</span><flux:button wire:click="bulkSetActive" size="sm">Set active</flux:button><flux:button wire:click="bulkArchive" size="sm" variant="danger">Archive</flux:button></div>@endif
    <x-admin.panel class="overflow-hidden p-0"><div class="overflow-x-auto"><table class="w-full min-w-[52rem] text-sm"><thead class="bg-zinc-50 text-left text-zinc-500 dark:bg-zinc-950/50"><tr><th class="p-4"><span class="sr-only">Select</span></th><th class="p-4"><button wire:click="sortBy('title')" class="font-semibold">Product</button></th><th class="p-4">Status</th><th class="p-4">Inventory</th><th class="p-4">Type</th><th class="p-4">Vendor</th><th class="p-4"><button wire:click="sortBy('updated_at')" class="font-semibold">Updated</button></th></tr></thead><tbody class="divide-y divide-zinc-200 dark:divide-zinc-800">@forelse($this->products as $product)<tr wire:key="product-{{ $product->id }}" class="hover:bg-zinc-50 dark:hover:bg-zinc-800/50"><td class="p-4"><flux:checkbox wire:model.live="selectedIds" value="{{ $product->id }}" aria-label="Select {{ $product->title }}" /></td><td class="p-4"><a href="/admin/products/{{ $product->id }}/edit" wire:navigate class="font-medium hover:underline">{{ $product->title }}</a><div class="text-xs text-zinc-500">{{ $product->variants_count }} variants</div></td><td class="p-4"><flux:badge :color="$product->status->value === 'active' ? 'green' : ($product->status->value === 'archived' ? 'red' : 'zinc')">{{ str($product->status->value)->headline() }}</flux:badge></td><td class="p-4">{{ $product->variants->sum(fn($variant) => $variant->inventoryItem?->quantity_on_hand ?? 0) }}</td><td class="p-4">{{ $product->product_type ?: '—' }}</td><td class="p-4">{{ $product->vendor ?: '—' }}</td><td class="p-4 text-zinc-500">{{ $product->updated_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="7" class="p-12 text-center"><flux:icon.cube class="mx-auto mb-3 size-10 text-zinc-400" /><flux:heading>No products found</flux:heading><flux:text>Adjust your filters or create your first product.</flux:text></td></tr>@endforelse</tbody></table></div><div class="border-t border-zinc-200 p-4 dark:border-zinc-800">{{ $this->products->links() }}</div></x-admin.panel>
</x-admin.page>
