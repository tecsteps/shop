<div><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Catalog</p><h1 class="mt-2 text-3xl font-bold">Products</h1></div><a href="{{ route('admin.products.create') }}" class="rounded-full bg-blue-600 px-5 py-3 text-sm font-semibold text-white" wire:navigate>Add product</a></div>@if ($message)<div class="mt-5 rounded-lg bg-green-50 px-4 py-3 text-sm font-semibold text-green-700">{{ $message }}</div>@endif<div class="mt-8 flex flex-wrap gap-3"><input aria-label="Search products" wire:model.live.debounce.300ms="search" placeholder="Search products" class="rounded-lg border border-zinc-300 bg-white px-4 py-3"><input aria-label="Filter by vendor" wire:model.live.debounce.300ms="vendor" placeholder="Vendor" class="rounded-lg border border-zinc-300 bg-white px-4 py-3"><select aria-label="Filter by product type" wire:model.live="productType" class="rounded-lg border border-zinc-300 bg-white px-4 py-3"><option value="all">All types</option><option value="Apparel">Apparel</option><option value="Accessories">Accessories</option><option value="Shoes">Shoes</option></select><select aria-label="Filter by status" wire:model.live="status" class="rounded-lg border border-zinc-300 bg-white px-4 py-3"><option value="active">Active</option><option value="draft">Draft</option><option value="archived">Archived</option><option value="all">All statuses</option></select>@if (count($selectedIds))<flux:button variant="danger" wire:click="bulkArchive" wire:confirm="Archive the selected products?">Archive selected ({{ count($selectedIds) }})</flux:button>@endif</div><div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm dark:bg-zinc-900"><div class="overflow-x-auto"><table class="w-full min-w-[720px] text-left text-sm"><thead class="bg-zinc-50 dark:bg-zinc-800"><tr><th class="px-5 py-4"><input type="checkbox" wire:model.live="selectAll" aria-label="Select all products"></th><th class="px-5 py-4">Product</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Inventory</th><th class="px-5 py-4">Price</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-zinc-200 dark:divide-zinc-800">@forelse ($products as $product)<tr wire:key="admin-product-{{ $product->id }}"><td class="px-5 py-4"><input type="checkbox" wire:model.live="selectedIds" value="{{ $product->id }}" aria-label="Select {{ $product->title }}"></td><td class="px-5 py-4"><a class="font-semibold text-blue-600" href="{{ route('admin.products.edit', $product) }}" wire:navigate>{{ $product->title }}</a><p class="text-xs text-zinc-500">{{ $product->vendor }}</p></td><td class="px-5 py-4"><span class="rounded-full bg-zinc-100 px-2.5 py-1 text-xs font-semibold">{{ ucfirst($product->status->value) }}</span></td><td class="px-5 py-4">{{ $product->variants->sum(fn ($variant) => $variant->inventory?->availableQuantity() ?? 0) }}</td><td class="px-5 py-4">€{{ number_format(($product->defaultVariant()?->price_amount ?? 0) / 100, 2) }}</td><td class="px-5 py-4 text-right">@if ($product->status->value !== 'archived')<button wire:click="archive({{ $product->id }})" class="text-sm font-semibold text-red-600">Archive</button>@endif</td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-zinc-500">No products found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $products->links() }}</div></div>
