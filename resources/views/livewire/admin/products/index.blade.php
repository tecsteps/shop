<div class="flex flex-col gap-6">
    <div><flux:breadcrumbs><flux:breadcrumbs.item :href="route('admin.dashboard')">Home</flux:breadcrumbs.item><flux:breadcrumbs.item>Products</flux:breadcrumbs.item></flux:breadcrumbs><div class="flex items-center justify-between gap-4"><flux:heading size="xl">Products</flux:heading><flux:button :href="route('admin.products.create')" wire:navigate variant="primary" icon="plus">Add product</flux:button></div></div>
    <div class="grid gap-3 sm:grid-cols-2"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search products..." /><flux:select wire:model.live="statusFilter"><flux:select.option value="all">All statuses</flux:select.option><flux:select.option value="draft">Draft</flux:select.option><flux:select.option value="active">Active</flux:select.option><flux:select.option value="archived">Archived</flux:select.option></flux:select></div>
    @if ($selectedIds)<div class="flex flex-wrap items-center gap-3 rounded-lg bg-zinc-100 p-3 dark:bg-zinc-800"><flux:text>{{ count($selectedIds) }} selected</flux:text><flux:button wire:click="setStatus('active')" size="sm">Set active</flux:button><flux:button wire:click="setStatus('archived')" size="sm" variant="danger">Archive</flux:button></div>@endif
    <div class="overflow-x-auto rounded-xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900"><table class="w-full text-sm"><thead class="bg-zinc-50 text-left text-zinc-500 dark:bg-zinc-900"><tr><th class="p-3"></th><th class="p-3">Product</th><th class="p-3">Status</th><th class="p-3">Inventory</th><th class="p-3">Type</th><th class="p-3">Updated</th></tr></thead><tbody wire:loading.class="opacity-50">@forelse ($products as $product)<tr wire:key="product-{{ $product->id }}" class="border-t border-zinc-100 dark:border-zinc-800"><td class="p-3"><flux:checkbox wire:model.live="selectedIds" value="{{ $product->id }}" /></td><td class="p-3"><a href="{{ route('admin.products.edit', $product) }}" wire:navigate class="font-medium hover:underline">{{ $product->title }}</a><div class="text-xs text-zinc-500">{{ $product->vendor ?: 'No vendor' }}</div></td><td class="p-3"><flux:badge size="sm" :color="$product->status->value === 'active' ? 'green' : 'zinc'">{{ ucfirst($product->status->value) }}</flux:badge></td><td class="p-3">{{ $product->variants->sum(fn ($variant) => $variant->inventoryItem?->quantity_on_hand ?? 0) }}</td><td class="p-3">{{ $product->product_type ?: '—' }}</td><td class="p-3">{{ $product->updated_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center"><flux:heading>No products found</flux:heading><flux:text>Start building your catalog by adding a product.</flux:text></td></tr>@endforelse</tbody></table></div>{{ $products->links() }}
</div>