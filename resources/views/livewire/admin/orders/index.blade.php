<x-admin.page title="Orders" subtitle="Review payments, fulfillment, and customer orders.">
    <x-admin.panel><div class="grid gap-3 md:grid-cols-[1fr_14rem]"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search order or email…" aria-label="Search orders" /><flux:select wire:model.live="statusFilter" aria-label="Filter orders"><flux:select.option value="all">All orders</flux:select.option><flux:select.option value="paid">Paid</flux:select.option><flux:select.option value="pending">Pending</flux:select.option><flux:select.option value="unfulfilled">Unfulfilled</flux:select.option><flux:select.option value="fulfilled">Fulfilled</flux:select.option><flux:select.option value="refunded">Refunded</flux:select.option></flux:select></div></x-admin.panel>
    <x-admin.panel class="overflow-hidden p-0"><div class="overflow-x-auto"><table class="w-full min-w-[56rem] text-sm"><thead class="bg-zinc-50 text-left text-zinc-500 dark:bg-zinc-950/50"><tr><th class="p-4">Order</th><th class="p-4">Date</th><th class="p-4">Customer</th><th class="p-4">Total</th><th class="p-4">Payment</th><th class="p-4">Fulfillment</th></tr></thead><tbody class="divide-y divide-zinc-200 dark:divide-zinc-800">@forelse($this->orders as $order)<tr wire:key="order-{{ $order->id }}"><td class="p-4"><a href="/admin/orders/{{ $order->id }}" wire:navigate class="font-semibold hover:underline">{{ $order->order_number }}</a></td><td class="p-4 text-zinc-500">{{ $order->placed_at?->format('M j, Y H:i') }}</td><td class="p-4"><div>{{ $order->customer?->name ?: 'Guest' }}</div><div class="text-xs text-zinc-500">{{ $order->email }}</div></td><td class="p-4 font-medium">{{ \Illuminate\Support\Number::currency($order->total_amount / 100, in: $order->currency) }}</td><td class="p-4"><flux:badge :color="$order->financial_status->value === 'paid' ? 'green' : ($order->financial_status->value === 'pending' ? 'amber' : 'zinc')">{{ str($order->financial_status->value)->headline() }}</flux:badge></td><td class="p-4"><flux:badge :color="$order->fulfillment_status->value === 'fulfilled' ? 'green' : 'zinc'">{{ str($order->fulfillment_status->value)->headline() }}</flux:badge></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-zinc-500">No orders found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-zinc-200 p-4 dark:border-zinc-800">{{ $this->orders->links() }}</div></x-admin.panel>
</x-admin.page>
