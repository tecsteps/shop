<div class="flex flex-col gap-6"><div><flux:breadcrumbs><flux:breadcrumbs.item :href="route('admin.dashboard')">Home</flux:breadcrumbs.item><flux:breadcrumbs.item>Orders</flux:breadcrumbs.item></flux:breadcrumbs><flux:heading size="xl">Orders</flux:heading></div><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search order number or email..." /><div class="flex gap-4 overflow-x-auto border-b border-zinc-200 dark:border-zinc-800">@foreach (['all', 'pending', 'paid', 'fulfilled', 'cancelled', 'refunded'] as $status)<button wire:key="order-filter-{{ $status }}" wire:click="$set('statusFilter', '{{ $status }}')" @class(['border-b-2 px-1 py-3 text-sm capitalize', 'border-zinc-900 font-semibold dark:border-white' => $statusFilter === $status, 'border-transparent text-zinc-500' => $statusFilter !== $status])>{{ $status }}</button>@endforeach</div><div class="overflow-x-auto rounded-xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900"><table class="w-full text-sm"><thead><tr class="text-left text-zinc-500"><th class="p-3">Order</th><th>Date</th><th>Customer</th><th>Payment</th><th>Fulfillment</th><th class="text-right">Total</th></tr></thead><tbody>@forelse ($orders as $order)<tr wire:key="order-{{ $order->id }}" class="border-t border-zinc-100 dark:border-zinc-800"><td class="p-3"><a href="{{ route('admin.orders.show', $order) }}" wire:navigate class="font-medium">{{ $order->order_number }}</a></td><td>{{ $order->placed_at?->format('M j, Y g:i A') }}</td><td>{{ $order->customer?->name ?? $order->email }}</td><td><flux:badge size="sm" :color="$order->financial_status->value === 'paid' ? 'green' : 'zinc'">{{ ucfirst(str_replace('_', ' ', $order->financial_status->value)) }}</flux:badge></td><td><flux:badge size="sm">{{ ucfirst($order->fulfillment_status->value) }}</flux:badge></td><td class="text-right">{{ number_format($order->total_amount / 100, 2) }} {{ $order->currency }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center">No orders found.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</div>