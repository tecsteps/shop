<x-admin.page title="Collections" subtitle="Group products for merchandising and navigation.">
    <x-slot:actions><flux:button href="/admin/collections/create" wire:navigate variant="primary" icon="plus">Add collection</flux:button></x-slot:actions>
    <x-admin.panel><div class="grid gap-3 md:grid-cols-[1fr_14rem]"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search collections…" aria-label="Search collections" /><flux:select wire:model.live="statusFilter" aria-label="Filter status"><flux:select.option value="all">All statuses</flux:select.option><flux:select.option value="active">Active</flux:select.option><flux:select.option value="draft">Draft</flux:select.option><flux:select.option value="archived">Archived</flux:select.option></flux:select></div></x-admin.panel>
    <x-admin.panel class="overflow-hidden p-0"><div class="overflow-x-auto"><table class="w-full min-w-[38rem] text-sm"><thead class="bg-zinc-50 text-left text-zinc-500 dark:bg-zinc-950/50"><tr><th class="p-4">Title</th><th class="p-4">Products</th><th class="p-4">Status</th><th class="p-4">Updated</th><th class="p-4"><span class="sr-only">Actions</span></th></tr></thead><tbody class="divide-y divide-zinc-200 dark:divide-zinc-800">@forelse($this->collections as $collection)<tr wire:key="collection-{{ $collection->id }}"><td class="p-4"><a href="/admin/collections/{{ $collection->id }}/edit" wire:navigate class="font-medium hover:underline">{{ $collection->title }}</a></td><td class="p-4">{{ $collection->products_count }}</td><td class="p-4"><flux:badge :color="$collection->status->value === 'active' ? 'green' : ($collection->status->value === 'archived' ? 'red' : 'zinc')">{{ str($collection->status->value)->headline() }}</flux:badge></td><td class="p-4 text-zinc-500">{{ $collection->updated_at->diffForHumans() }}</td><td class="p-4 text-end"><flux:button wire:click="deleteCollection({{ $collection->id }})" wire:confirm="Delete this collection?" variant="ghost" icon="trash" aria-label="Delete {{ $collection->title }}" /></td></tr>@empty<tr><td colspan="5" class="p-12 text-center"><flux:heading>Create your first collection</flux:heading><flux:text>Collections make products easier to discover.</flux:text></td></tr>@endforelse</tbody></table></div><div class="border-t border-zinc-200 p-4 dark:border-zinc-800">{{ $this->collections->links() }}</div></x-admin.panel>
</x-admin.page>
