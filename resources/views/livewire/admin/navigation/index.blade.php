<div class="space-y-6"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Online store</p><h1 class="mt-2 text-3xl font-bold">Navigation</h1><p class="mt-2 text-zinc-600">Manage menus and links shown across the storefront.</p></div><div class="grid gap-6 lg:grid-cols-[260px_1fr]"><div class="space-y-2 rounded-2xl bg-white p-4 shadow-sm ring-1 ring-zinc-200"><h2 class="px-3 pb-2 text-sm font-semibold uppercase tracking-wide text-zinc-500">Menus</h2>@foreach($menus as $menu)<button type="button" wire:click="selectMenu({{ $menu->id }})" class="flex w-full items-center justify-between rounded-lg px-3 py-3 text-left {{ $menuId === $menu->id ? 'bg-blue-50 text-blue-700' : 'hover:bg-zinc-50' }}"><span>{{ $menu->name }}</span><span class="text-xs text-zinc-500">{{ $menu->items->count() }}</span></button>@endforeach</div><div class="space-y-6"><form wire:submit="saveMenu" class="grid gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-zinc-200 sm:grid-cols-2"><flux:input wire:model="menuName" label="Menu name" required /><flux:input wire:model="menuHandle" label="Handle" required /><div class="sm:col-span-2"><flux:button type="submit" variant="primary">Save menu</flux:button></div></form>@if($menu)<div class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-zinc-200"><h2 class="font-semibold">{{ $menu->name }} items</h2><div class="mt-4 divide-y divide-zinc-100">@forelse($menu->items as $item)<div class="flex items-center justify-between py-3"><div><p class="font-medium">{{ $item->label }}</p><p class="text-sm text-zinc-500">{{ $item->url }}</p></div><flux:button size="sm" variant="ghost" wire:click="deleteItem({{ $item->id }})" wire:confirm="Remove this menu item?">Remove</flux:button></div>@empty<p class="py-5 text-sm text-zinc-500">No links yet.</p>@endforelse</div><form wire:submit="addItem" class="mt-6 grid gap-3 border-t border-zinc-200 pt-5 sm:grid-cols-3"><flux:input wire:model="label" label="Label" required /><flux:input wire:model="url" label="URL" required /><div class="flex items-end"><flux:button type="submit" variant="primary">Add link</flux:button></div></form></div>@endif</div></div></div>
