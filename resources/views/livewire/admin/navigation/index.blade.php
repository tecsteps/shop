<x-admin.page title="Navigation" subtitle="Build storefront menus and links."><div class="grid gap-6 lg:grid-cols-[18rem_1fr]"><x-admin.panel heading="Menus"><form wire:submit="createMenu" class="mb-4 space-y-3"><flux:input wire:model="menuTitle" label="New menu" /><flux:button type="submit" size="sm" variant="primary" icon="plus">Create</flux:button></form><div class="grid gap-1">@foreach($this->menus as $menu)<button wire:key="menu-{{ $menu->id }}" wire:click="selectMenu({{ $menu->id }})" type="button" @class(['rounded-lg px-3 py-2 text-left text-sm hover:bg-zinc-100 dark:hover:bg-zinc-800','bg-zinc-100 font-semibold dark:bg-zinc-800'=>$menuId===$menu->id])>{{ $menu->title }}</button>@endforeach</div></x-admin.panel><x-admin.panel heading="Menu items">@if($menuId)<form wire:submit="addItem" class="mb-5 grid gap-3 sm:grid-cols-[1fr_1fr_auto]"><flux:input wire:model="itemLabel" label="Label" /><flux:input wire:model="itemUrl" label="URL" /><div class="flex items-end"><flux:button type="submit" variant="primary" icon="plus">Add</flux:button></div></form>@php($activeMenu = $this->menus->firstWhere('id', $menuId))<div class="grid gap-2">@forelse($activeMenu?->items ?? [] as $item)<div wire:key="nav-item-{{ $item->id }}" class="flex items-center gap-3 rounded-xl border border-zinc-200 p-3 dark:border-zinc-800"><flux:icon.bars-3 class="size-5 text-zinc-400" /><div class="flex-1"><div class="font-medium">{{ $item->label }}</div><div class="text-xs text-zinc-500">{{ $item->url }}</div></div><flux:button wire:click="removeItem({{ $item->id }})" variant="ghost" icon="trash" /></div>@empty<flux:text>This menu has no links.</flux:text>@endforelse</div>@else<flux:text>Create or select a menu.</flux:text>@endif</x-admin.panel></div></x-admin.page>
