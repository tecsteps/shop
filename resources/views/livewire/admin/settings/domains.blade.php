<div class="rounded-2xl bg-white shadow-sm ring-1 ring-zinc-200"><div class="flex flex-wrap items-center justify-between gap-3 border-b border-zinc-200 p-6"><flux:heading size="lg">Connected domains</flux:heading><flux:button size="sm" variant="primary" wire:click="$set('showModal', true)">Add domain</flux:button></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-zinc-200 text-left text-sm"><thead class="bg-zinc-50"><tr><th class="px-5 py-3">Hostname</th><th class="px-5 py-3">Type</th><th class="px-5 py-3">Primary</th><th class="px-5 py-3">TLS</th><th class="px-5 py-3"><span class="sr-only">Actions</span></th></tr></thead><tbody class="divide-y divide-zinc-100">@forelse ($domains as $domain)<tr wire:key="domain-{{ $domain->id }}"><td class="px-5 py-4 font-medium">{{ $domain->hostname }}</td><td class="px-5 py-4"><flux:badge color="zinc">{{ ucfirst($domain->type->value) }}</flux:badge></td><td class="px-5 py-4">@if ($domain->is_primary)<flux:badge color="green">Primary</flux:badge>@else<flux:button size="sm" variant="ghost" wire:click="setPrimary({{ $domain->id }})">Set primary</flux:button>@endif</td><td class="px-5 py-4 text-zinc-600">{{ ucfirst($domain->tls_mode ?: 'managed') }}</td><td class="px-5 py-4 text-right"><flux:button size="sm" variant="ghost" wire:click="removeDomain({{ $domain->id }})" wire:confirm="Remove this domain?">Delete</flux:button></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-zinc-500">No custom domains configured.</td></tr>@endforelse</tbody></table></div>
    <flux:modal name="add-domain" class="max-w-md" wire:model="showModal"><form wire:submit="addDomain" class="space-y-6"><flux:heading size="lg">Add domain</flux:heading><flux:input wire:model="newHostname" label="Hostname" placeholder="shop.example.com" required /><flux:select wire:model="newType" label="Type"><option value="storefront">Storefront</option><option value="admin">Admin</option><option value="api">API</option></flux:select><div class="flex justify-end gap-3"><flux:button type="button" variant="ghost" wire:click="$set('showModal', false)">Cancel</flux:button><flux:button type="submit" variant="primary">Add domain</flux:button></div></form></flux:modal>
</div>
