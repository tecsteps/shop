<div class="max-w-3xl"><a href="{{ route('admin.settings') }}" class="text-sm text-blue-600" wire:navigate>← Settings</a><h1 class="mt-4 text-3xl font-bold">Shipping settings</h1>@if ($message)<div class="mt-5 rounded-lg bg-green-50 px-4 py-3 text-sm font-semibold text-green-700">{{ $message }}</div>@endif<form wire:submit="save" class="mt-8 grid gap-4 rounded-2xl bg-white p-6 shadow-sm sm:grid-cols-3"><input wire:model="zoneName" placeholder="Zone name" class="rounded-lg border border-zinc-300 px-4 py-3"><input wire:model="rateName" placeholder="Rate name" class="rounded-lg border border-zinc-300 px-4 py-3"><input type="number" wire:model="amount" placeholder="Amount in cents" class="rounded-lg border border-zinc-300 px-4 py-3"><button class="sm:col-span-3 rounded-full bg-blue-600 px-6 py-3 font-semibold text-white">Save shipping rate</button></form><div class="mt-8 space-y-4">@foreach ($zones as $zone)<div wire:key="zone-{{ $zone->id }}" class="rounded-2xl bg-white p-6 shadow-sm"><h2 class="font-semibold">{{ $zone->name }}</h2>@foreach ($zone->rates as $rate)<p wire:key="rate-{{ $rate->id }}" class="mt-2 text-sm text-zinc-600">{{ $rate->name }} · €{{ number_format($rate->price_amount / 100, 2) }}</p>@endforeach</div>@endforeach</div></div>
