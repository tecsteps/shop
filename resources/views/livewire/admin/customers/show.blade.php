<div><a href="{{ route('admin.customers.index') }}" class="text-sm text-blue-600" wire:navigate>← Customers</a><h1 class="mt-4 text-3xl font-bold">{{ $customer->name }}</h1><p class="mt-2 text-zinc-500">{{ $customer->email }}</p><div class="mt-8 grid gap-8 lg:grid-cols-2"><section class="rounded-2xl bg-white p-6 shadow-sm"><h2 class="text-lg font-semibold">Order history</h2>@forelse ($customer->orders as $order)<a wire:key="customer-order-{{ $order->id }}" href="{{ route('admin.orders.show', $order) }}" class="mt-4 flex justify-between border-b border-zinc-100 py-3" wire:navigate><span>{{ $order->order_number }}</span><span>€{{ number_format($order->total_amount / 100, 2) }}</span></a>@empty<p class="mt-4 text-sm text-zinc-500">No orders.</p>@endforelse</section><section class="rounded-2xl bg-white p-6 shadow-sm"><h2 class="text-lg font-semibold">Addresses</h2>@forelse ($customer->addresses as $address)<div wire:key="customer-address-{{ $address->id }}" class="mt-4 rounded-lg bg-zinc-50 p-4 text-sm">{{ $address->address_json['address1'] ?? '' }}, {{ $address->address_json['city'] ?? '' }}</div>@empty<p class="mt-4 text-sm text-zinc-500">No addresses.</p>@endforelse</section></div></div>
