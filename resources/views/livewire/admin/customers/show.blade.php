<x-admin.page :title="$customer->name" :subtitle="$customer->email">
    <div class="grid gap-6 xl:grid-cols-3"><div class="space-y-6 xl:col-span-2"><x-admin.panel heading="Order history"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="text-left text-zinc-500"><tr><th class="py-2">Order</th><th>Date</th><th>Status</th><th>Total</th></tr></thead><tbody>@forelse($customer->orders as $order)<tr wire:key="customer-order-{{ $order->id }}" class="border-t border-zinc-200 dark:border-zinc-800"><td class="py-3"><a href="/admin/orders/{{ $order->id }}" wire:navigate class="font-medium hover:underline">{{ $order->order_number }}</a></td><td>{{ $order->placed_at?->format('M j, Y') }}</td><td><flux:badge>{{ str($order->financial_status->value)->headline() }}</flux:badge></td><td>{{ \Illuminate\Support\Number::currency($order->total_amount / 100, in: $order->currency) }}</td></tr>@empty<tr><td colspan="4" class="py-8 text-center text-zinc-500">No orders yet.</td></tr>@endforelse</tbody></table></div></x-admin.panel></div><div class="space-y-6"><x-admin.panel heading="Customer"><dl class="space-y-3 text-sm"><div><dt class="text-zinc-500">Email</dt><dd>{{ $customer->email }}</dd></div><div><dt class="text-zinc-500">Marketing</dt><dd>{{ $customer->marketing_opt_in ? 'Subscribed' : 'Not subscribed' }}</dd></div></dl></x-admin.panel><x-admin.panel heading="Addresses"><div class="space-y-4">@forelse($customer->addresses as $address)<address wire:key="address-{{ $address->id }}" class="whitespace-pre-line rounded-lg bg-zinc-50 p-3 text-sm not-italic dark:bg-zinc-800">{{ collect($address->address_json)->filter()->implode("\n") }}</address>@empty<flux:text>No saved addresses.</flux:text>@endforelse</div></x-admin.panel></div></div>
</x-admin.page>
