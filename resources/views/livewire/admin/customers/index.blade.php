<div class="space-y-6">
    <div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Relationships</p><flux:heading size="xl" level="1" class="mt-2">Customers</flux:heading><flux:text class="mt-2" variant="subtle">Review customer accounts, order history, and lifetime value.</flux:text></div>
    <flux:input wire:model.live.debounce.300ms="search" placeholder="Search by name or email…" aria-label="Search customers" class="max-w-md" />
    <div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-zinc-200"><table class="min-w-full divide-y divide-zinc-200 text-left text-sm"><caption class="sr-only">Customers</caption><thead class="bg-zinc-50"><tr><th class="px-5 py-3">Name</th><th class="px-5 py-3">Email</th><th class="px-5 py-3">Orders</th><th class="px-5 py-3">Total spent</th><th class="px-5 py-3">Created</th></tr></thead><tbody class="divide-y divide-zinc-100">@forelse ($customers as $customer)<tr wire:key="customer-{{ $customer->id }}" class="hover:bg-zinc-50"><td class="px-5 py-4"><a href="{{ route('admin.customers.show', $customer) }}" wire:navigate class="font-semibold text-blue-700 hover:underline">{{ $customer->name ?: 'Unnamed customer' }}</a></td><td class="px-5 py-4 text-zinc-600">{{ $customer->email }}</td><td class="px-5 py-4">{{ number_format($customer->orders_count) }}</td><td class="px-5 py-4">{{ app('current_store')->default_currency }} {{ number_format(($customer->orders_sum_total_amount ?? 0) / 100, 2) }}</td><td class="whitespace-nowrap px-5 py-4 text-zinc-600">{{ $customer->created_at?->format('M j, Y') }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-zinc-500">No customers match your search.</td></tr>@endforelse</tbody></table></div>
    <div>{{ $customers->links() }}</div>
</div>
