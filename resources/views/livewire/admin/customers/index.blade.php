<div class="flex flex-col gap-6"><div><flux:breadcrumbs><flux:breadcrumbs.item :href="route('admin.dashboard')">Home</flux:breadcrumbs.item><flux:breadcrumbs.item>Customers</flux:breadcrumbs.item></flux:breadcrumbs><flux:heading size="xl">Customers</flux:heading></div><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search customers..." /><div class="overflow-x-auto rounded-xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900"><table class="w-full text-sm"><thead><tr class="text-left text-zinc-500"><th class="p-3">Name</th><th>Email</th><th>Orders</th><th>Total spent</th><th>Joined</th></tr></thead><tbody>@forelse ($customers as $customer)<tr wire:key="customer-{{ $customer->id }}" class="border-t border-zinc-100 dark:border-zinc-800"><td class="p-3"><a href="{{ route('admin.customers.show', $customer) }}" wire:navigate class="font-medium">{{ $customer->name }}</a></td><td>{{ $customer->email }}</td><td>{{ $customer->orders_count }}</td><td>{{ number_format(($customer->orders_sum_total_amount ?? 0) / 100, 2) }} {{ app('current_store')->default_currency }}</td><td>{{ $customer->created_at->format('M j, Y') }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center">No customers found.</td></tr>@endforelse</tbody></table></div>{{ $customers->links() }}</div>