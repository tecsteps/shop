<x-admin.page title="Customers" subtitle="Customer profiles, order history, and marketing preferences.">
    <x-admin.panel><div class="grid gap-3 md:grid-cols-[1fr_14rem]"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search name or email…" aria-label="Search customers" /><flux:select wire:model.live="marketingFilter" aria-label="Marketing filter"><flux:select.option value="all">All customers</flux:select.option><flux:select.option value="subscribed">Subscribed</flux:select.option><flux:select.option value="not_subscribed">Not subscribed</flux:select.option></flux:select></div></x-admin.panel>
    <x-admin.panel class="overflow-hidden p-0"><div class="overflow-x-auto"><table class="w-full min-w-[48rem] text-sm"><thead class="bg-zinc-50 text-left text-zinc-500 dark:bg-zinc-950/50"><tr><th class="p-4">Customer</th><th class="p-4">Marketing</th><th class="p-4">Orders</th><th class="p-4">Total spent</th><th class="p-4">Joined</th></tr></thead><tbody class="divide-y divide-zinc-200 dark:divide-zinc-800">@forelse($this->customers as $customer)<tr wire:key="customer-{{ $customer->id }}"><td class="p-4"><a href="/admin/customers/{{ $customer->id }}" wire:navigate class="font-medium hover:underline">{{ $customer->name }}</a><div class="text-xs text-zinc-500">{{ $customer->email }}</div></td><td class="p-4"><flux:badge :color="$customer->marketing_opt_in ? 'green' : 'zinc'">{{ $customer->marketing_opt_in ? 'Subscribed' : 'Not subscribed' }}</flux:badge></td><td class="p-4">{{ $customer->orders_count }}</td><td class="p-4">{{ \Illuminate\Support\Number::currency(($customer->orders_sum_total_amount ?? 0) / 100, in: $this->currentStore()->default_currency) }}</td><td class="p-4 text-zinc-500">{{ $customer->created_at->format('M j, Y') }}</td></tr>@empty<tr><td colspan="5" class="p-12 text-center text-zinc-500">No customers found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-zinc-200 p-4 dark:border-zinc-800">{{ $this->customers->links() }}</div></x-admin.panel>
</x-admin.page>
