<x-admin.page title="Apps" subtitle="Extend your store with installed integrations."><div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($this->apps as $app)@php($installation = $app->installations->first())<x-admin.panel wire:key="app-{{ $app->id }}"><div class="mb-4 flex size-12 items-center justify-center rounded-xl bg-zinc-100 dark:bg-zinc-800"><flux:icon.squares-2x2 /></div><flux:heading>{{ $app->name }}</flux:heading><flux:text class="mt-1">Store integration</flux:text><div class="mt-4">@if($installation && $installation->status->value === 'active')<div class="flex items-center justify-between"><flux:badge color="green">Installed</flux:badge><flux:button wire:click="uninstall({{ $installation->id }})" size="sm" variant="danger">Uninstall</flux:button></div>@else<flux:button wire:click="install({{ $app->id }})" size="sm" variant="primary">Install</flux:button>@endif</div></x-admin.panel>@empty<x-admin.panel><flux:heading>No apps available</flux:heading><flux:text>The app catalog is currently empty.</flux:text></x-admin.panel>@endforelse</div></x-admin.page>
