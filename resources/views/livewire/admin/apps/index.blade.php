<div class="space-y-6"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Platform</p><h1 class="mt-2 text-3xl font-bold">Apps</h1><p class="mt-2 text-zinc-600">Review installed integrations for this store.</p></div><div class="grid gap-5 md:grid-cols-2">@forelse($installations as $installation)<article class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-zinc-200"><div class="flex justify-between gap-3"><div><h2 class="font-semibold">{{ $installation->app?->name ?? 'App' }}</h2><p class="mt-1 text-sm text-zinc-500">{{ $installation->app?->description }}</p></div><span class="text-xs text-zinc-500">{{ ucfirst($installation->status) }}</span></div><flux:button class="mt-5" size="sm" variant="ghost" wire:click="uninstall({{ $installation->id }})" wire:confirm="Uninstall this app?">Uninstall</flux:button></article>@empty<div class="rounded-2xl bg-white p-10 text-center text-zinc-500">No apps installed.</div>@endforelse</div>@if($availableApps->isNotEmpty())<div class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-zinc-200"><h2 class="font-semibold">Available apps</h2><div class="mt-4 grid gap-4 sm:grid-cols-2">@foreach($availableApps as $app)<div class="rounded-xl border border-zinc-200 p-4"><p class="font-medium">{{ $app->name }}</p><p class="mt-1 text-sm text-zinc-500">{{ $app->description }}</p><p class="mt-3 text-xs text-zinc-500">Installation is managed by the app OAuth flow.</p></div>@endforeach</div></div>@endif</div>
