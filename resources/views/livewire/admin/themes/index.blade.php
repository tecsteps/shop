<x-admin.page title="Themes" subtitle="Customize and publish the storefront appearance.">
    <div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($this->themes as $theme)<x-admin.panel wire:key="theme-{{ $theme->id }}"><div class="mb-4 aspect-video rounded-xl bg-linear-to-br from-zinc-100 to-zinc-300 dark:from-zinc-700 dark:to-zinc-900"></div><div class="flex items-start justify-between gap-3"><div><flux:heading>{{ $theme->name }}</flux:heading><flux:text>Version {{ $theme->version }}</flux:text></div><flux:badge :color="$theme->status->value === 'published' ? 'green' : 'zinc'">{{ str($theme->status->value)->headline() }}</flux:badge></div><div class="mt-4 flex flex-wrap gap-2"><flux:button href="/admin/themes/{{ $theme->id }}/editor" wire:navigate size="sm">Customize</flux:button><flux:button wire:click="duplicate({{ $theme->id }})" size="sm">Duplicate</flux:button>@if($theme->status->value !== 'published')<flux:button wire:click="publish({{ $theme->id }})" size="sm" variant="primary">Publish</flux:button>@endif</div></x-admin.panel>@empty<x-admin.panel><flux:heading>No themes</flux:heading><flux:text>Install a theme to begin customizing your storefront.</flux:text></x-admin.panel>@endforelse</div>
</x-admin.page>
