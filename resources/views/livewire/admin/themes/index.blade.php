<div class="space-y-6"><div class="flex items-end justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Online store</p><h1 class="mt-2 text-3xl font-bold">Themes</h1><p class="mt-2 text-zinc-600">Preview, customize, and publish the storefront theme.</p></div></div><div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($themes as $theme)<article class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-zinc-200"><div class="flex items-start justify-between gap-3"><div><h2 class="font-semibold">{{ $theme->name }}</h2><p class="text-sm text-zinc-500">Version {{ $theme->version }}</p></div><span class="rounded-full bg-zinc-100 px-2.5 py-1 text-xs">{{ ucfirst($theme->status->value) }}</span></div><div class="mt-5 flex flex-wrap gap-2"><flux:button size="sm" :href="route('admin.themes.editor', $theme)" wire:navigate>Customize</flux:button>@if($theme->status->value !== 'published')<flux:button size="sm" variant="primary" wire:click="publish({{ $theme->id }})">Publish</flux:button>@endif<flux:button size="sm" variant="ghost" wire:click="duplicate({{ $theme->id }})">Duplicate</flux:button>@if($theme->status->value !== 'published')<flux:button size="sm" variant="ghost" wire:click="delete({{ $theme->id }})" wire:confirm="Delete this theme?">Delete</flux:button>@endif</div></article>@empty<div class="rounded-2xl bg-white p-10 text-center text-zinc-500">No themes have been created.</div>@endforelse</div></div>
