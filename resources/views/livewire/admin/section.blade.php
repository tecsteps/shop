<div><p class="text-sm font-semibold uppercase tracking-widest text-blue-600">Admin</p><h1 class="mt-2 text-3xl font-bold">{{ $heading }}</h1><p class="mt-4 max-w-2xl text-zinc-600">Manage your store’s {{ strtolower($heading) }} from this workspace.</p><div class="mt-8 overflow-hidden rounded-2xl bg-white shadow-sm">@if(count($rows) > 0)<div class="divide-y divide-zinc-100">@foreach($rows as $row)<div class="flex items-center justify-between gap-4 px-6 py-5"><div><p class="font-semibold text-zinc-900">{{ $row['title'] }}</p><p class="mt-1 text-sm text-zinc-500">{{ $row['subtitle'] }}</p></div><span class="text-sm font-medium text-zinc-600">{{ $row['value'] }}</span></div>@endforeach</div>@else<div class="p-8 text-sm text-zinc-600">No {{ strtolower($heading) }} records have been added yet.</div>@endif</div></div>
